name: Arduino Lib

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  Arduino-Checks:
    uses: sensirion/.github/.github/workflows/upt.arduino.check.yml@main
    with:
      expect-arduino-examples: true
      lint-lib-manager-check: update
      dependency-list: "[{'name':'Sensirion UPT Core'}, {'name':'NimBLE-Arduino'}, {'name':'Sensirion I2C SEN66'}, {'source-path': './'}]"
